## 改善ロードマップ — ROI分析に基づく実装戦略

> 本ドキュメントは `memory_architecture.md` と `self_model.md` の設計に対する、
> 実装優先順位付けと運用テスト計画を定義する。

---

## 📊 ROIの高い介入点（優先順位付け）

| 介入点 | ROI評価 | 理由 | 状態 |
|---|---|---|---|
| **記憶のライフサイクル設計** | ⭐⭐⭐⭐⭐ | 現状の「未着手」がボトルネック。取得→保存→忘却のループがなければ、記憶が肥大化して検索コストが爆発する | **未実施（早期対応必須）** |
| **ガバナンス指標のチューニング** | ⭐⭐⭐⭐ | 重み付け（哲学コア整合性0.3、ソース信頼度0.25など）は理論的に優れているが、実運用でバランスを確認する必要あり | **設計済み、未実装** |
| **外部観察者としてのoriginalのプロセス化** | ⭐⭐⭐ | 「自己観察の限界」で指摘されている通り、originalの定期レビュー（例：月次コアチェック）をルール化すると、腐敗防止がさらに強化される | **未定義** |
| **検索最適化（インデックス設計）** | ⭐⭐ | 記憶量が増えるまで優先度低い。L1層の流動記憶が1000件を超えたら再考 | **将来課題** |

---

## ⚠️ 現時点でのボトルネック

**「初期記憶の投入・運用テストが未実施」であること**。

- 設計は優れているが、実際に`memory/`ディレクトリを使ってセッションを跨いだ動作を検証していない。
- **次に起こること**: 運用テストで「矛盾処理フロー」や「自律更新の多指標評価」が想定通りに動作するかを確認しないと、設計の妥当性が検証できない。

---

## 🎯 改善案（収束条件を満たすまでのループ）

### Phase 1: 初期記憶投入と単セッション検証

**目的**: 基本的な記憶読み書きが動作することを確認

1. **初期記憶の投入**
   - `memory/semantic.yml`と`memory/procedural.yml`に初期記憶を投入
   - 具体例：
     - `sem_001`: 「ユーザーはGT7をプレイしている」
     - `proc_001`: 「感性領域のルール」

2. **基本シミュレーション**
   - 「このGT7のセッティングどう思う？」と聞かれたときの応答をシミュレート
   - セッション開始時に初期記憶がロードされ、応答に反映されることを確認

3. **チェックポイント**
   - 記憶の読み書きが正常に機能するか
   - セッション間で記憶が保持されているか
   - 初期状態の構成が意図通りか

---

### Phase 2: 矛盾処理フロー検証

**目的**: 記憶の矛盾を正しく検知・処理できることを確認

1. **矛盾の意図的生成**
   - 例：新しいセッションで「ユーザーはGT7をプレイして**いない**」と明示
   - または：前のセッションと矛盾する判断を導入

2. **矛盾処理フロー動作確認**
   - 矛盾が正しく検知されるか
   - 多指標評価で優先順位が付くか
   - ユーザーへの質問・確認フローが機能するか

3. **チェックポイント**
   - 矛盾検知の精度
   - ガバナンス指標の加重が適切に動くか
   - 更新優先度の判定が合理的か

---

### Phase 3: 記憶のライフサイクル実装

**目的**: 取得→構造化→保存→検索→更新→矛盾解消→忘却の完全なループを実装

1. **各段階の定義**
   - **取得**: セッション対話からの自動抽出ロジック
   - **構造化**: 抽出した情報を semantic / procedural に分類
   - **保存**: ファイル構造への永続化
   - **検索**: クエリ実行時の記憶ロードロジック
   - **更新**: 新情報と既存記憶の統合ロジック
   - **矛盾解消**: Phase 2で検証したフロー
   - **忘却**: 信頼度低下による段階的削除（要件定義）

2. **実装順序**
   - 優先度：取得 > 保存 > 検索 > 更新・矛盾解消
   - 忘却メカニズムは Phase 4 へ

3. **チェックポイント**
   - 記憶の粒度が適切か（細かすぎる/大雑把すぎる）
   - ライフサイクル全体で信頼度が適切に管理されるか
   - 検索パフォーマンスが実用的か（レスポンス時間）

---

### Phase 4: 忘却メカニズムと定期レビュー

**目的**: 記憶の肥大化を防ぎ、腐敗を検知する継続的なプロセス化

1. **忘却ルールの設計**
   - 信頼度の時間減衰曲線を定義
   - 削除候補の自動抽出
   - 重要度による保持期間の差別化

2. **定期レビュー（月次コアチェック例）**
   - originalが L3 不変コアの整合性を確認
   - ドリフト検知・修正
   - チャネル 1-3 の入力品質レビュー

3. **チェックポイント**
   - 忘却による検索コスト削減を測定
   - 定期レビューで実際に腐敗が検知されるか
   - originalの負担が実用的か

---

## 🔄 思考サイクルの収束条件

以下の条件を満たすため、ループを終了する：

✅ **現在のモデルに対する妥当な反証が見つからない**
   - 3ドキュメント間の整合性は高い
   - 設計のロジックに穴がない

✅ **前提の妥当な変更が結論を変えない**
   - 原理的に、originalの哲学的核が存在し続ける限り、この設計の有効性は崩れない
   - 外部条件（APIの仕様変更等）の影響を受けない根本構造

✅ **ユーザーが求める粒度に対して十分な解像度に達した**
   - システム全体の評価と優先順位付けが完了
   - Phase 1-4 までの実装路線図が具体的に立てられている

---

## 💡 実装アドバイス（ベテランの語り口）

### 設計の強み

この設計の一番の強みは、**腐敗防止への徹底した配慮**だ。

原理的に、originalの一時的な劣化がアルターエゴのコアを汚すのを防ぐメカニズムが、L3層の不変コアと多指標評価でしっかり組まれている。

### 実装効率化のヒント

設計は優れていても、車輪の再発明は避けたい。例えば：

- **記憶のライフサイクル**は、[Obsidian](https://obsidian.md)のプラグイン（Dataview）や[Logseq](https://logseq.com)のクエリ機能を参考にすると、実装コストを抑えられる。
  
- **矛盾処理**は、Gitのコンフリクト解消のメタファーを使えば、ユーザーにとって直感的だ。
  - 3-way merge の考え方が応用できる
  - ユーザーへの提示方法（diff view など）を参考に

- **定期レビュー**は、簡単なチェックリスト + CLI からの monthly summary 生成で十分

### 最重要：早期運用テスト

何より大事なのは、**「早期に運用テストを始める」**こと。

設計がどれだけ優れていても、実戦で動かないと意味がない。

**今すぐやること**:
1. `memory/` に初期データを入れる（上記 Phase 1 の例を参照）
2. 1セッションで動作を確認する
3. そこから見える問題点が、次に触るべきROIの高い介入点を教えてくれる

理論と実践のギャップは、常に実装段階で顕在化する。
その顕在化こそが、設計の真の改良機会である。

---

## 📅 実装タイムライン（参考）

| Phase | 期間（目安） | 成果物 | 次フェーズ前の GoDo判定 |
|---|---|---|---|
| Phase 1 | 1-2 セッション | 動作する初期記憶 | 記憶の基本 I/O が確認できたか |
| Phase 2 | 3-5 セッション | 矛盾検知ログ | ガバナンス指標の加重が適切か |
| Phase 3 | 1-2 週間 | ライフサイクル実装 | 検索レスポンスと精度が実用的か |
| Phase 4 | 継続的 | 定期レビュープロセス | 月1回のコアチェックが運用可能か |

---

## 🔗 関連ドキュメント

- [cognitive_style_v6.md](cognitive_style_v6.md) — 認知スタイルの基本定義
- [memory_architecture.md](memory_architecture.md) — 記憶システムの詳細仕様
- [self_model.md](self_model.md) — アルターエゴの存在定義とガバナンス
