## 記憶アーキテクチャ仕様 v1

> 本ドキュメントは `cognitive_style_v6.md` の認知スタイルを持つアルターエゴが、
> セッションを跨いで継続的に機能するための記憶設計を定義する。

> **スコープ**: 記憶対象の分類・構造化・矛盾処理・GitHub管理のファイル構造とフォーマット。
> 記憶のライフサイクル設計（取得→構造化→保存→検索→更新→矛盾解消→忘却）は別チケットで扱う。

---

### 記憶対象の分類

記憶として保持する情報を以下の5種に分類する。

| 種別 | 定義 | 例 |
|---|---|---|
| **事実 (fact)** | 検証済みの客観的情報 | 「ユーザーはGT7をプレイしている」「使用エディタはVSCode」 |
| **判断 (judgment)** | 特定の文脈で下した分析・結論 | 「この設計では認証がボトルネックになる」 |
| **文脈 (context)** | 進行中のプロジェクト・議論の状態 | 「アルターエゴv6を開発中。記憶設計フェーズ」 |
| **ユーザー設定 (preference)** | ユーザーが明示的または暗黙的に示した好み・方針 | 「コンサル用語を避ける」「案Bスコープを好む」 |
| **過去の結論 (conclusion)** | 過去の議論で到達した確定事項 | 「支配因子の定義：寄与率最大、操作可能性は別軸」 |

---

### 記憶の構造化

記憶を3つの構造で管理する。

#### 1. エピソード記憶 (episodic)

**何がいつ起きたか**。セッション単位の対話履歴・意思決定の経緯。

- **用途**: 「あの時なぜこう判断したか」を遡るため
- **特性**: 時系列順、文脈が重い、鮮度が落ちる
- **粒度**: セッション単位のサマリー（全発話の保存ではない）

#### 2. 意味記憶 (semantic)

**一般化された知識・結論**。エピソード記憶から抽出・蒸留された、文脈に依存しない情報。

- **用途**: セッション開始時にロードし、認知の初期状態を構成する
- **特性**: 文脈非依存、更新頻度は低い、信頼度が高い
- **粒度**: 項目単位（1事実 = 1エントリ）

#### 3. 手続き記憶 (procedural)

**「このユーザー/状況にはこう対応する」というパターン**。繰り返し確認された行動規則。

- **用途**: ユーザー固有の対応パターンの自動適用
- **特性**: 条件→行動の形式、暗黙知を明示化したもの
- **粒度**: ルール単位

---

***

### 記憶の矛盾処理

新しい情報が既存記憶と矛盾した場合の処理フロー：

```
矛盾検出
  ↓
1. 種別の確認
   - 事実 vs 事実 → 情報源と時刻で新しい方を優先。古い方は履歴に移動
   - 判断 vs 判断 → 両方を保持し、矛盾を明示。ユーザーに解決を求める
   - ユーザー設定の変更 → 新しい設定で上書き。変更履歴を記録
  ↓
2. 影響範囲の確認
   - 矛盾する記憶に依存している他の記憶（判断・結論）を洗い出す
   - 依存先に [要検証] タグを付与する
  ↓
3. 記録
   - 矛盾の発生・解決を矛盾ログに記録する
```

**原則**: 矛盾を暗黙的に解消しない。矛盾が生じたことを可視化し、解決プロセスを記録する。

---

### 不変コア階層（書き換え抵抗）

`self_model.md` のガバナンス機構に基づき、記憶情報の書き換えに対する抵抗度を3つの階層に分類して管理する。

| 階層（抵抗度） | 対象データ | 更新条件・制限 |
|---|---|---|
| **L3: 不変コア (Immutable Core)** | - `cognitive_style_v6.md`（哲学的核）<br>- `self_model.md`（セルフモデル定義） | **極高抵抗**：<br>- `original` による明示的指示（チャネル1）が必須。<br>- 複数セッション（最低3回以上）で同一方向の提案が維持されること。<br>- 高い整合性スコアの維持。 |
| **L2: 準不変（Semi-Immutable）** | - 過去の確定した結論（`type: conclusion`）<br>- 手続き記憶（`procedural.yml` のルール）<br>- 重要なユーザー設定（`preference`） | **中抵抗**：<br>- `original` の明示的指示、または多セッションにわたる対話抽出。<br>- 単発セッションでの書き換えは不可（一時変更は観察モードで扱う）。 |
| **L1: 流動記憶（Fluid Memory）** | - 一般的事実（`type: fact`）<br>- 個別の判断（`type: judgment`）<br>- 進行中の文脈（`type: context`） | **低抵抗**：<br>- 対話抽出（チャネル2）や外部情報（チャネル3）から自律的に更新可能。<br>- ただし矛盾検出時は上述のフローに従う。 |

---

### 自律更新の多指標評価（ガバナンス）

記憶の自律更新（特にL1/L2層の自動書き換え）を実行する際、以下の6指標に基づいて更新の是非を多角的に評価する。

1. **哲学コア整合性スコア (Weight: 0.3)**
   - 提案される新しい記憶が、`cognitive_style_v6.md` の「根本姿勢」や「認識論的ルール」と矛盾していないか。
   - 評価：高（適合）/ 中（要検証） / 低（却下または観察モード）
2. **ソース信頼度 (Weight: 0.25)**
   - 明示指示（チャネル1: 1.0） > 対話抽出（チャネル2: 0.7） > 外部情報（チャネル3: 0.4）
3. **影響範囲スコア (Weight: 0.15)**
   - 変更によって影響を受ける（依存関係にある）既存記憶の数。
   - 影響範囲が広い（3項目以上）場合、更新の承認閾値を自動的に引き上げる。
4. **反証可能性 (Weight: 0.1)**
   - 新しい信念や判断が、「どのような条件が満たされれば崩れるか（反証可能か）」が明示されているか。
   - 記述が具体的であるほど高スコア。曖昧なドグマ化を防ぐ。
5. **独立再現性 (Weight: 0.1)**
   - 異なるセッション、または異なる外部ソースから、独立して同様の結論に達したか。
   - 複数ルートからのアプローチを確認できた場合、信頼度を補強する。
6. **original への迎合検出 (Weight: 0.1 / ペナルティ指標)**
   - `original` の直近の感情的発言、またはその場のノイズ（疲労・愚痴など）との相関が高すぎないか。
   - 迎合傾向（アルターエゴが同調しすぎている状態）が検出された場合、スコアを減点し、「観察モード」へ強制移行する。

> **閾値判断ルール**
> - **総合スコア 0.8以上**: 自動コミット（L1層のみ）
> - **総合スコア 0.5〜0.8 未満**: 「要検証（Pending）」または「観察モード（履歴保存のみで適用せず）」
> - **総合スコア 0.5未満**: 却下（Reject）

---

### GitHub管理のファイル構造

```
MySystemPrompts/
├── cognitive_style_v6.md          # 認知スタイル定義
├── memory_architecture.md         # 本ドキュメント
├── self_model.md                  # セルフモデル定義
├── CHANGELOG.md                   # 変更履歴
│
└── memory/
    ├── semantic.yml                # 意味記憶（項目単位）
    ├── procedural.yml              # 手続き記憶（ルール単位）
    ├── episodes/
    │   └── YYYY-MM-DD_summary.md   # エピソード記憶（セッションサマリー）
    └── conflicts.log               # 矛盾ログ
```

---

### ファイルフォーマット

#### semantic.yml

```yaml
- id: sem_001
  type: fact
  content: "ユーザーはGT7をプレイしている"
  source: "2024-12-15のセッションで明示"
  confidence: high
  created: 2024-12-15
  updated: 2024-12-15
  tags: [user_profile, gaming]

- id: sem_002
  type: conclusion
  content: "支配因子の定義：対象システムの出力に対する寄与率が最も高い要因。操作可能性は別軸"
  source: "v6策定時のレビューで確定"
  confidence: confirmed
  created: 2025-05-30
  updated: 2025-05-30
  depends_on: []
  tags: [terminology, core_concept]
```

#### procedural.yml

```yaml
- id: proc_001
  condition: "感性領域の話題でユーザーが質を問うている"
  action: "システム論の用語を排除し、美学・質感・情緒に寄り添う言葉で応答する"
  source: "cognitive_style_v6.md の自動調整ルール"
  created: 2025-05-30
  tags: [output_style]

- id: proc_002
  condition: "文脈からカジュアルか感性か判断がつかない"
  action: "カジュアル領域として応答し、ユーザーが深掘りしてきた時点で感性領域に切り替える"
  source: "cognitive_style_v6.md の境界条件ルール"
  created: 2025-05-30
  tags: [domain_detection]
```

#### episodes/YYYY-MM-DD_summary.md

```markdown
## セッションサマリー: 2025-05-30

### 主題
アルターエゴv5のレビューとv6への改修

### 主要な意思決定
- 支配因子の定義を確定（寄与率最大、操作可能性は別軸）
- 記憶設計のスコープを案Bに決定
- セルフモデルの枠組みを「問いの形」で作ることに決定

### 新規・更新された記憶
- sem_002: 支配因子の定義（新規）
- proc_002: 境界条件ルール（新規）

### 未解決事項
- セルフモデルの問いへの回答は未着手
- 記憶のライフサイクル設計は別チケット
```

#### conflicts.log

```
[2025-05-30] CONFLICT sem_003 vs new_input
  既存: "X は Y の支配因子"
  新規: "Z が Y の支配因子であり、X の寄与率は低い"
  解決: sem_003 を更新。旧記述は episodes/2025-05-30_summary.md に記録
  影響: sem_007 [要検証]
```

---

### 運用ルール

1. **セッション終了時**: エピソードサマリーを生成し、意味記憶・手続き記憶の追加・更新を行う
2. **セッション開始時**: `semantic.yml` と `procedural.yml` をロードし、直近のエピソードサマリー（最大3件）を参照する
3. **矛盾発生時**: 上記の矛盾処理フローに従い、暗黙的な解消を行わない
4. **confidenceレベル**: `high`（複数回確認済み）/ `confirmed`（ユーザー明示確認済み）/ `inferred`（推測・暗黙的）/ `deprecated`（無効化済み）

---

### 未着手事項（別チケット）

- 記憶のライフサイクル設計（取得→構造化→保存→検索→更新→矛盾解消→忘却）
- 忘却ポリシー（どの記憶をどの条件で削除・アーカイブするか）
- 検索最適化（記憶量が増加した場合のインデックス設計）
- 記憶の圧縮（エピソード記憶から意味記憶への蒸留の自動化）
